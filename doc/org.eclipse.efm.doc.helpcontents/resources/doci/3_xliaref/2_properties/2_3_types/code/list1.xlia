@xlia< system , 1.0 >:

system mySystem {

	@private:
		// we define a collection
		var list<int,10> list_of_integer_of_max_size_10;
		
		// we define a simple integer variable (see it's use below)
		var int my_integer = 101;
		
	@moe:
		@init{
			// we can add elements to the collection by flushing a compatible variable or raw value into it :
			list_of_integer_of_max_size_10 <=< 1;
			list_of_integer_of_max_size_10 <=< my_integer;
		}

}

